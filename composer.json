{
  "require": {
    "inc2734/mimizuku": "*",
    "composer/installers": "^1.2"
  },
  "extra": {
    "installer-paths": {
      "../{$name}": ["type:wordpress-theme"]
    }
  },
  "scripts": {
    "build-mimizuku": [
      "cd ../mimizuku && composer install --no-dev && npm install && npm run gulp build"
    ],
    "post-install-cmd": [
      "composer build-mimizuku"
    ],
    "post-update-cmd": [
      "composer build-mimizuku"
    ]
  }
}
